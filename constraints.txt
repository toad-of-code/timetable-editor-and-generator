═══════════════════════════════════════════════════════════════════
              TIMETABLE SOLVER — CONSTRAINT REFERENCE
═══════════════════════════════════════════════════════════════════

File: src/solver/constraints.ts
Solver: (1+1)-ES (Evolutionary Strategy)
Fitness = (Hard Violations × Hard Penalty) + (Gap Penalty × Gap Weight) 
          + (Room Util Penalty × Util Weight) + (Prof Penalty × Prof Weight)


───────────────────────────────────────────────────────────────────
                        HARD CONSTRAINTS
───────────────────────────────────────────────────────────────────
  Hard violations MUST be exactly 0 for the timetable to be feasible.

  1. TIME BOUNDARY
     Function: countTimeBoundaryViolations()
     Rule:     A session must absolutely not exceed the final slot of the day.
               endSlot = startSlot + duration - 1 must be ≤ 8 (18:30).

  2. BREAK / LUNCH CROSSING
     Function: countBreakViolations()
     Rule:     A continuous multi-slot session (like a 2-hour lab) must NOT 
               span across a structural break.
               • Slot 2 → Slot 3  (10:50 – 11:00 break)
               • Slot 4 → Slot 5  (13:00 – 14:30 lunch)

  3. ROOM NON-OVERLAP
     Function: countRoomOverlaps()
     Rule:     A physical room can host at most ONE session at any (day, slot).
               Multiple sessions placed in the same room simultaneously cause clashes.

  4. PROFESSOR NON-OVERLAP
     Function: countProfessorOverlaps()
     Rule:     A distinct professor can teach at most ONE session at any (day, slot).

  5. STUDENT GROUP (SECTION) NON-OVERLAP
     Function: countGroupOverlaps()
     Rule:     A specific section of students can attend at most ONE session at 
               any given (day, slot).
     Exception: Sessions from the SAME basket (Synced or Free) are allowed to overlap 
                because students pick only one subject per basket.

  6. ELECTIVE RULES & SYNCHRONIZATION
     Function: countElectiveSyncViolations()
     Rule A (Sync): All sessions in a "Synced Basket" (e.g., HSMC) MUST be 
                    scheduled at the exact same (day, slot).
     Rule B (Free): Electives in "Free Baskets" can be scheduled independently.
     Rule C (Anti-Clash): Sessions from DIFFERENT baskets must never overlap, 
                          ensuring students can pick one from each without conflict.

  7. LAB ROOM FOR PRACTICALS
     Function: countLabRoomViolations()
     Rule:     Practical sessions MUST be assigned to rooms with room_type 'Lab'.

  8. HOME ROOM FOR LECTURES/TUTORIALS
     Function: countHomeRoomViolations()
     Rule:     Core lectures and tutorials MUST occur in the designated 'Home Room'.
               (Practicals and Electives are exempt).

  9. WMC (WHOLE BATCH) VS SECTION
     Function: countWMCSectionOverlaps()
     Rule:     If a WMC session is scheduled, no individual sub-section belonging 
               to that batch can have a separate core class at that time.

  10. 2+1 LECTURE FORMAT
     Function: countTwoOneLectureViolations()
     Rule:     For core (non-elective) subjects with lectures >= 2, the lecture
               hours are split into:
                 • Exactly ONE 2-hour double-lecture block (lecturePairIndex = 0)
                 • All remaining hours as individual 1-hour sessions (lecturePairIndex = -1)
               Example breakdown:
                 lectures=2  →  one 2-hr session (total: 1 session)
                 lectures=3  →  one 2-hr + one 1-hr (total: 2 sessions)
                 lectures=4  →  one 2-hr + two 1-hr (total: 3 sessions)
               The constraint enforces that no two lecture sessions for the
               same subject+group fall on the same day. This guarantees the
               2-hr block and the 1-hr remainder(s) are spread across different
               days, preventing overloading a single day with the same subject.
               Elective lectures, tutorials, and practicals are NOT affected.


───────────────────────────────────────────────────────────────────
                        SOFT CONSTRAINTS
───────────────────────────────────────────────────────────────────
  Soft penalties optimize for quality but do not invalidate the timetable.

  1. STUDENT GAP PENALTY (COMPRESSION)
     Function: computeGapPenalty()
     Rule:     Minimizes empty "free" slots between a student groups first 
               and last class of the day to compress their schedule.

  2. ROOM UTILIZATION PENALTY
     Function: computeRoomUtilizationPenalty()
     Rule:     Encourages efficient use of space. Penalties apply if room fill 
               ratio is < 60% (under-util) or > 120% (over-util).
     Config:   roomUtilizationWeight = 0.8

  3. PROFESSOR SAME-HALF PENALTY
     Function: computeProfessorSameHalfPenalty()
     Rule:     Encourages compact faculty schedules. A penalty is added if a 
               professor teaches in both Morning (1-4) and Afternoon (5-8) halves.
     Note:     Locked sessions are included to detect cross-semester splits, 
               but penalty only applies if a movable session is present.
     Config:   professorSameHalfWeight = 5.0


───────────────────────────────────────────────────────────────────
                      TIME MODEL (1-HOUR SLOTS)
───────────────────────────────────────────────────────────────────

  Slot 1:  08:50 – 09:50
  Slot 2:  09:50 – 10:50
    [BREAK: 10:50 – 11:00]
  Slot 3:  11:00 – 12:00
  Slot 4:  12:00 – 13:00
    [LUNCH: 13:00 – 14:30]
  Slot 5:  14:30 – 15:30
  Slot 6:  15:30 – 16:30
  Slot 7:  16:30 – 17:30
  Slot 8:  17:30 – 18:30

  Days: Mon–Fri (1–5)


───────────────────────────────────────────────────────────────────
                      MUTATOR BEHAVIORS
───────────────────────────────────────────────────────────────────

  • Mutate Time: Uses dynamic (1+1)-ES σ (sigma) via the 1/5th Rule.
  • Sync Lock: Mutations forcibly drag entire synced elective baskets.
  • Swap: Allowed for core sessions of identical duration.
  • Break Snap: Automatically snaps mutated slots to safe start times.


───────────────────────────────────────────────────────────────────

make groups in baskets

mtech spec core or elec every subject
uniform groups among basktes

all metch busy  cannot  with btech lab -soft cons
same half not teacher-soft cons

//minor btech is mba core

9-11 lbs mtech lec
11-1 btech max lecture and mtech labs

finl yera lec 9-11 and 4;30 -6:30

shailendra 

ek prof multiple class
can be shared

prof not want to teach a class , we can see free room for that 